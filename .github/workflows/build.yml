name: Build

on:
  push:
    branches:
      - development

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup NodeJS 18 🔃
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install 🔧
        run: npm install

      - name: Build 🏗️
        run: npm run build
